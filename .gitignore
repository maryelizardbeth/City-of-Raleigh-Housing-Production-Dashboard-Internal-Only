# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini

# Office temp lock files
~$*

# Local env / secrets (none expected, but never commit these)
.env
*.local

# Local backup snapshots (editor/manual saves)
*.bak.*
